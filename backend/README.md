## Setup
```bash
# install
$ yarn

# Setup env file for database
$ cp prisma/.env.example prisma/.env

$ yarn start:dev

## Swagger
Swagger UI is available on `http://localhost:8888/api`

